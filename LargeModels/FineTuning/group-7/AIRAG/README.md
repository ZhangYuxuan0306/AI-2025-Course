# 🤖 RAG问答系统

基于LangChain框架实现的完整检索增强生成（Retrieval-Augmented Generation, RAG）问答系统。

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![LangChain](https://img.shields.io/badge/LangChain-0.3.0-green.svg)](https://python.langchain.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

> **🚀 [快速开始指南](QUICKSTART.md)** | **🐛 [修复说明](FIXES.md)** | **📖 [详细文档](docs/USAGE.md)**

## 📋 目录

- [功能特性](#功能特性)
- [系统架构](#系统架构)
- [快速开始](#快速开始)
- [详细文档](#详细文档)
- [评估指标](#评估指标)
- [演示界面](#演示界面)
- [参考文献](#参考文献)

## ✨ 功能特性

### 基础功能
- ✅ **文档处理**: 支持PDF、TXT、DOCX等多种格式
- ✅ **智能分块**: 基于RecursiveCharacterTextSplitter的语义分块
- ✅ **向量索引**: 支持FAISS和Chroma向量数据库
- ✅ **多种检索器**: FAISS向量检索、BM25文本检索、混合检索
- ✅ **带引用生成**: 生成的答案包含来源引用和相关度评分

### 进阶功能
- ✅ **RAGAS评测**: 自动化评估忠实度、答案相关性、上下文精确度等
- ✅ **检索器对比**: 对比FAISS vs BM25性能和效果
- ✅ **失败案例分析**: 误差归因（检索错误、排序错误、生成错误）
- ✅ **性能监控**: 延迟、吞吐量等在线指标
- ✅ **离线指标**: 召回率、精确度、Top-K命中率、MRR等

### 演示界面
- ✅ **Web UI**: 基于Gradio的美观交互式界面
- ✅ **CLI**: 功能完整的命令行交互界面
- ✅ **一键启动**: 自动化环境配置和依赖安装

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                     RAG问答系统架构                        │
└─────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  文档加载     │ ───> │  文档分块     │ ───> │  向量索引     │
│  (Loader)    │      │  (Splitter)  │      │  (FAISS)     │
└──────────────┘      └──────────────┘      └──────────────┘
                                                    │
                                                    ↓
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  答案生成     │ <─── │  相关检索     │ <─── │  用户查询     │
│  (Generator) │      │  (Retriever) │      │  (Query)     │
└──────────────┘      └──────────────┘      └──────────────┘
      │                     ↑
      │                     │
      ↓                     │
┌──────────────┐      ┌──────────────┐
│  带引用答案   │      │  BM25/混合    │
│  (Answer)    │      │  (Optional)  │
└──────────────┘      └──────────────┘
```

## 🚀 快速开始

### 环境要求

- Python 3.8+
- 8GB+ RAM (推荐16GB)
- GPU (可选，用于加速嵌入编码)

### 安装步骤

#### Windows

```bash
# 克隆项目
git clone <your-repo-url>
cd 人工智能RAG

# 一键启动（自动安装依赖）
start.bat
```

#### Linux/Mac

```bash
# 克隆项目
git clone <your-repo-url>
cd 人工智能RAG

# 赋予执行权限
chmod +x start.sh

# 一键启动
./start.sh
```

### 手动安装

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境 (Windows)
venv\Scripts\activate
# 或 (Linux/Mac)
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 复制环境配置
cp .env.example .env

# 编辑.env文件，配置API密钥等
```

### 准备文档

将需要索引的文档放在 `data/documents/` 目录下：

```
data/
  └── documents/
      ├── document1.pdf
      ├── document2.txt
      └── document3.docx
```

## 📖 详细文档

### 使用方式

#### 1. Web界面 (推荐)

```bash
python run.py --mode web
```

访问 http://localhost:7860 即可使用Web界面。

**功能:**
- 系统初始化配置
- 交互式问答
- 检索器对比
- 实时结果展示

#### 2. CLI命令行

```bash
# 交互模式
python run.py --mode cli

# 单次查询
python cli_demo.py --question "什么是人工智能？" --retriever faiss

# 对比检索器
python cli_demo.py --question "机器学习的应用" --compare
```

**CLI命令:**
- 直接输入问题进行查询
- `/compare <问题>` - 对比检索器
- `/retriever <faiss|bm25|hybrid>` - 切换检索器
- `/topk <数字>` - 设置Top-K
- `/help` - 显示帮助
- `/quit` - 退出

#### 3. 索引文档

```bash
python run.py --mode index --documents ./data/documents
```

#### 4. 运行评估

```bash
python run.py --mode eval
```

评估报告将保存在 `data/results/` 目录。

### 项目结构

```
人工智能RAG/
├── src/                          # 核心源代码
│   ├── __init__.py
│   ├── document_loader.py        # 文档加载和分块
│   ├── vector_store.py           # 向量数据库管理
│   ├── retriever.py              # 多种检索器实现
│   ├── generator.py              # 答案生成
│   └── evaluation.py             # 评估和失败分析
├── data/                         # 数据目录
│   ├── documents/                # 原始文档
│   ├── vectordb/                 # 向量数据库
│   ├── results/                  # 评估结果
│   └── logs/                     # 日志文件
├── web_demo.py                   # Web界面
├── cli_demo.py                   # CLI界面
├── run.py                        # 主运行脚本
├── evaluate_rag.py               # 完整评估脚本
├── config.py                     # 配置管理
├── requirements.txt              # 依赖包
├── .env.example                  # 环境配置示例
├── start.bat                     # Windows启动脚本
├── start.sh                      # Linux/Mac启动脚本
└── README.md                     # 项目文档
```

## 📊 评估指标

### 离线指标

#### 检索指标
- **精确度 (Precision)**: 检索结果的准确性
- **召回率 (Recall)**: 相关文档的覆盖率
- **F1分数**: 精确度和召回率的调和平均
- **Hit Rate**: Top-K命中率
- **MRR (Mean Reciprocal Rank)**: 平均倒数排名

#### RAGAS指标
- **Faithfulness**: 答案忠实度（基于上下文）
- **Answer Relevancy**: 答案相关性
- **Context Precision**: 上下文精确度
- **Context Recall**: 上下文召回率

### 在线指标

- **平均延迟**: 单次查询的平均响应时间
- **吞吐量**: 每秒处理的查询数
- **P95/P99延迟**: 95%/99%分位数延迟

### 误差归因

系统自动分析失败案例，归因到：
- **检索错误**: 未检索到相关文档
- **排序错误**: 相关文档排序靠后
- **生成错误**: 生成答案不准确
- **上下文错误**: 文档分块不当

## 🎨 演示界面

### Web界面

![Web Demo](docs/images/web_demo.png)

**特点:**
- 🎯 直观的配置界面
- 💬 实时问答交互
- 📚 来源文档展示
- 🔄 检索器对比视图

### CLI界面

```
🚀 初始化RAG系统...
✅ 加载了 1523 个文档块
✅ 向量索引创建完成
✅ RAG系统初始化完成！

>>> 什么是人工智能？

🔍 查询中...

╭──────────────── 💬 回答 ────────────────╮
│ 问题: 什么是人工智能？                    │
│                                          │
│ 答案:                                    │
│ 人工智能(AI)是计算机科学的一个分支，旨在  │
│ 创建能够模拟人类智能的系统...[来源1]      │
╰──────────────────────────────────────────╯

📚 参考来源:
  [来源 1]
  内容: 人工智能的定义...
  文件: ai_introduction.pdf
  相关度: 0.8542
```

## ⚙️ 配置说明

编辑 `.env` 文件进行配置：

```bash
# 模型配置
MODEL_TYPE=local              # local 或 api
OPENAI_API_KEY=sk-xxx         # API密钥
OPENAI_BASE_URL=https://...   # API地址

# 嵌入模型
EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5

# 向量数据库
VECTOR_DB_TYPE=faiss          # faiss 或 chroma

# 检索配置
TOP_K=5
CHUNK_SIZE=500
CHUNK_OVERLAP=50
```

### 嵌入模型推荐

- **中文**: `BAAI/bge-large-zh-v1.5` (推荐)
- **多语言**: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- **英文**: `sentence-transformers/all-MiniLM-L6-v2`

## 🔧 高级用法

### 自定义文档处理

```python
from src.document_loader import DocumentProcessor

processor = DocumentProcessor(
    chunk_size=1000,
    chunk_overlap=100
)
chunks = processor.process_documents("path/to/docs")
```

### 自定义检索器

```python
from src.retriever import RetrieverManager

retriever = RetrieverManager(vs_manager)
retriever.setup_bm25(chunks)

# 混合检索
results = retriever.hybrid_retrieve(
    query="your question",
    k=5,
    faiss_weight=0.7  # FAISS权重
)
```

### 自定义评估

```python
from src.evaluation import RAGEvaluator

evaluator = RAGEvaluator()
dataset = evaluator.prepare_evaluation_dataset(
    questions=["Q1", "Q2"],
    answers=["A1", "A2"],
    contexts=[["C1"], ["C2"]]
)
results = evaluator.evaluate_rag_system(dataset)
```

## 📈 性能优化建议

### 1. 向量检索优化
- 使用GPU加速嵌入编码
- 调整FAISS索引类型（IVF、HNSW等）
- 批量编码文档

### 2. 检索质量优化
- 调整chunk_size和chunk_overlap
- 使用混合检索策略
- 添加重排序（Reranker）

### 3. 生成质量优化
- 使用更强的LLM
- 优化提示模板
- 添加思维链（Chain-of-Thought）

## 🐛 常见问题

### Q: 依赖安装失败？

**问题**: `ERROR: No matching distribution found for faiss-cpu==1.8.0`

**解决方案**:
```bash
# 方案1: 使用更新的requirements.txt（已修复）
pip install -r requirements.txt

# 方案2: 手动安装核心依赖
pip install langchain langchain-community
pip install faiss-cpu>=1.9.0
pip install sentence-transformers gradio
```

### Q: 中文显示乱码？

**问题**: Windows命令行显示 `�活虚拟环境...`

**解决方案**: 已在 `start.bat` 中自动设置 UTF-8 编码。如果仍有问题：
```bash
# 手动设置编码
chcp 65001
```

### Q: 如何使用本地LLM？

**解决方案**: 编辑`.env`文件：
```bash
MODEL_TYPE=local
OPENAI_BASE_URL=http://localhost:8000/v1  # 本地API地址
```

### Q: 向量数据库如何选择？

**建议**: 
- **FAISS**: 适合本地部署，速度快，内存占用大
- **Chroma**: 适合持久化，自动管理，易于使用

### Q: 如何提高检索准确率？

**方法**:
1. 优化文档分块大小 (调整 `CHUNK_SIZE`)
2. 使用更好的嵌入模型 (如 `bge-large-zh-v1.5`)
3. 尝试混合检索策略 (选择 `hybrid`)
4. 增加Top-K值获取更多上下文

### Q: 模型下载慢或失败？

**解决方案**:
```bash
# 使用国内镜像
export HF_ENDPOINT=https://hf-mirror.com

# Windows
set HF_ENDPOINT=https://hf-mirror.com
```

### Q: RAGAS评估失败？

**原因**: RAGAS需要配置有效的LLM API

**解决方案**: 确保：
1. `.env`中配置了正确的API密钥
2. API地址可访问
3. 模型支持的token长度足够

## 📚 参考文献

- [RAGAS: Automated Evaluation of RAG](https://arxiv.org/abs/2309.15217)
- [ColBERTv2: Efficient Retrieval via Late Interaction](https://arxiv.org/abs/2112.01488)
- [LangChain RAG Tutorial](https://python.langchain.com/docs/tutorials/rag/)
- [LlamaIndex Framework](https://github.com/run-llama/llama_index)
- [Haystack RAG App](https://github.com/deepset-ai/haystack-rag-app)

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📄 许可证

本项目采用 [MIT License](LICENSE)。

## 👥 作者

- Your Name - [email@example.com](mailto:email@example.com)

## 🙏 致谢

感谢以下开源项目：
- [LangChain](https://github.com/langchain-ai/langchain)
- [RAGAS](https://github.com/explodinggradients/ragas)
- [FAISS](https://github.com/facebookresearch/faiss)
- [Gradio](https://github.com/gradio-app/gradio)

---

**⭐ 如果这个项目对你有帮助，请给一个Star！**

