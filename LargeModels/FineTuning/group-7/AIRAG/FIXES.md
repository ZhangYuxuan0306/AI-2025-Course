# 修复说明

## 已修复的问题

### 1. ✅ 中文编码问题

**问题**: Windows命令行显示乱码 `�活虚拟环境...`

**修复**:
```bat
# start.bat 第2行
chcp 65001 >nul 2>&1  # 完全抑制输出
```

### 2. ✅ faiss-cpu版本问题

**问题**: `ERROR: No matching distribution found for faiss-cpu==1.8.0`

**原因**: faiss-cpu 1.8.0 版本不存在

**修复**: 更新 `requirements.txt`
- 旧版本: `faiss-cpu==1.8.0`
- 新版本: `faiss-cpu>=1.9.0`

### 3. ✅ 依赖版本过于严格

**问题**: 固定版本可能导致版本冲突

**修复**: 使用版本范围替代固定版本
```txt
# 旧
langchain==0.3.0

# 新
langchain>=0.2.0,<0.4.0
```

### 4. ✅ 启动脚本改进

**改进内容**:
1. 自动升级pip
2. 更好的错误提示
3. 静默处理非关键输出
4. 添加虚拟环境激活检查

### 5. ✅ 文档更新

**更新内容**:
1. README.md - 添加详细故障排除
2. QUICKSTART.md - 新增快速开始指南
3. 删除不需要的 hello.py

## 现在可以正常使用

### 重新运行安装

```bash
# 删除旧的虚拟环境（如果存在问题）
rmdir /s /q venv

# 重新运行启动脚本
start.bat
```

### 预期结果

```
========================================
  RAG问答系统 - Windows启动脚本
========================================

[提示] 未找到虚拟环境，正在创建...
[成功] 虚拟环境创建完成
[提示] 激活虚拟环境...
[提示] 正在安装依赖包（首次运行需要较长时间）...

安装进度...

[成功] 依赖安装完成

请选择运行模式:
1. Web界面 (推荐)
2. CLI命令行
3. 索引文档
4. 运行评估
5. 退出

请输入选项 (1-5):
```

## 手动安装（如果启动脚本仍有问题）

```bash
# 1. 创建虚拟环境
python -m venv venv

# 2. 激活虚拟环境
venv\Scripts\activate

# 3. 升级pip
python -m pip install --upgrade pip

# 4. 安装依赖
pip install -r requirements.txt

# 5. 复制配置文件
copy .env.example .env

# 6. 运行测试
python test_system.py

# 7. 启动Web界面
python run.py --mode web
```

## 核心依赖列表

如果仍有问题，可以逐个安装核心依赖：

```bash
# 核心框架
pip install langchain langchain-community langchain-openai langchain-huggingface

# 向量数据库
pip install faiss-cpu chromadb

# 嵌入模型
pip install sentence-transformers transformers torch

# 文档处理
pip install pypdf python-docx

# 检索器
pip install rank-bm25

# 评测工具
pip install ragas

# Web UI
pip install gradio

# 工具库
pip install pandas numpy matplotlib seaborn
pip install python-dotenv tqdm loguru pydantic rich
```

## 验证安装

运行系统测试脚本：
```bash
python test_system.py
```

预期输出：
```
============================================================
RAG问答系统 - 系统测试
============================================================
测试1: 模块导入...
✓ 所有模块导入成功

测试2: 文档处理...
✓ 文档处理成功，共 XX 个文档块

...

通过率: 6/6 (100.0%)

🎉 所有测试通过！系统运行正常。
```

## 获取帮助

如果还有问题：

1. **查看日志**: `data/logs/*.log`
2. **运行测试**: `python test_system.py`
3. **查看文档**: 
   - [快速开始](QUICKSTART.md)
   - [使用指南](docs/USAGE.md)
   - [常见问题](README.md#常见问题)

## 技术支持

- 📧 Email: your.email@example.com
- 💬 Issues: [GitHub Issues]
- 📚 文档: [完整文档](README.md)

---

**更新时间**: 2025-10-19
**修复版本**: v1.0.1

